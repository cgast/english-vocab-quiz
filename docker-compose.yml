services:
  vokabeltrainer:
    build: .
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
      - DATA_DIR=/data
    volumes:
      - vokabeltrainer-data:/data
    restart: unless-stopped

volumes:
  vokabeltrainer-data:
